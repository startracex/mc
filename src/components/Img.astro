---
const {src } = Astro.props;
const base=import.meta.env.BASE_URL;
let currect = src;
if (!src.startsWith(base)) {
   currect = base + (src.startsWith('/') ? src.slice(1) : src);
}
---
<img src={currect} {...Astro.props}>
   <slot/>
</img>
